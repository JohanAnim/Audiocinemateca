<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_cast"
        android:title="Transmitir audio a Smart TV o altavoz inteligente"
        app:actionProviderClass="androidx.mediarouter.app.MediaRouteActionProvider"
        app:showAsAction="always" />

    <!-- More options menu -->
    <item
        android:id="@+id/action_more_options"
        android:icon="@drawable/ic_more_vert"
        android:title="Más opciones"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/action_playback_speed"
                android:title="Velocidad de reproducción"
                app:showAsAction="never" />
            <item
                android:id="@+id/action_equalizer"
                android:title="Ecualizador"
                app:showAsAction="never" />
            <item
                android:id="@+id/action_sleep_timer"
                android:title="Temporizador de apagado"
                app:showAsAction="never" />
            <item
                android:id="@+id/action_autoplay"
                android:title="Reproducción automática"
                android:checkable="true"
                app:showAsAction="never" />
        </menu>
    </item>
</menu>