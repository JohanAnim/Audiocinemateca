<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ListPreference
        android:key="theme"
        android:title="Tema de la aplicación"
        android:summary="%s"
        android:entries="@array/theme_entries"
        android:entryValues="@array/theme_values"
        android:defaultValue="system" />

    <ListPreference
        android:key="startup_tab"
        android:title="Pestaña de inicio"
        android:summary="%s"
        android:entries="@array/startup_tab_entries"
        android:entryValues="@array/startup_tab_values"
        android:defaultValue="catalog" />

    <ListPreference
        android:key="default_content_tab"
        android:title="Pestaña de contenido predeterminada"
        android:summary="%s"
        android:entries="@array/default_content_tab_entries"
        android:entryValues="@array/default_content_tab_values"
        android:defaultValue="peliculas" />

    <ListPreference
        android:key="default_filter"
        android:title="Orden de visualización predeterminado"
        android:summary="%s"
        android:entries="@array/default_filter_entries"
        android:entryValues="@array/default_filter_values"
        android:defaultValue="date_desc" />

    <SwitchPreferenceCompat
        android:key="auto_check_catalog"
        android:title="Comprobación automática del catálogo"
        android:summary="Buscar y aplicar actualizaciones del catálogo al iniciar la app."
        android:defaultValue="true" />

    <SwitchPreferenceCompat
        android:key="auto_check_app"
        android:title="Comprobación automática de la aplicación"
        android:summary="Mostrar un diálogo al iniciar la app si hay una nueva versión disponible."
        android:defaultValue="true" />

    <PreferenceCategory
        app:title="Permisos y Enlaces"
        app:key="cat_permissions">
        <Preference
            android:key="pref_battery_optimization"
            android:title="Optimización de batería"
            android:summary="Permite que la app funcione mejor en segundo plano (Recomendado)." />
        <Preference
            android:key="pref_deep_links"
            android:title="Abrir enlaces de Audiocinemateca"
            android:summary="Configura la app para abrir automáticamente los enlaces de audiocinemateca.com" />
    </PreferenceCategory>

    <PreferenceCategory
        app:title="Diagnóstico"
        app:key="cat_diag">
        <SwitchPreferenceCompat
            android:key="create_crash_log"
            android:title="Generar archivo de registro de errores (.log)"
            android:summary="Si la app falla, se guardará un reporte interno para compartirlo con el desarrollador."
            android:defaultValue="true" />
        <Preference
            android:key="share_latest_log"
            android:title="Compartir último reporte de error"
            android:summary="Envía el último archivo de registro generado por Telegram, WhatsApp u otra aplicación."
            app:dependency="create_crash_log" />
    </PreferenceCategory>

</PreferenceScreen>
